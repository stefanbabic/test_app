<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Neuen Beitrag schreiben..." %>
  </div>
  <%= f.submit "Posten", class: "btn btn-primary" %>
<% end %>
